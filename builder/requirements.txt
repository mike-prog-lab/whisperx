# WhisperX 3.8.1 — pulls pyannote>4.0, faster-whisper>1.1.1,
# ctranslate2>4.5, numpy>2.1, transformers>4.48 as transitive deps.
# torch 2.8.0 is pre-installed in the base Docker image.
whisperx @ git+https://github.com/m-bain/whisperx.git@v3.8.1

# Speaker recognition (ECAPA embeddings)
speechbrain

# RunPod serverless handler
runpod==1.7.13

# Utilities
pydub
python-dotenv
huggingface-hub
